let currentGridState = [];

const initGrid = () => {
  // const gridGenerator = new GridGenerator();
  // currentGridState = gridGenerator.generateGrid();
  // assignColorToBoxes('.box_element', colorPickerForBoxes.value);
  // assignColorToBoxes('.box_element--filled', colorPickerForFilledBoxes.value);
  // addChangeableColorOnHover('.box_element--filled');
  // listenForCountVerticallyAndHorizontallyAlignedBoxesValueEvents();
}

initGrid();
